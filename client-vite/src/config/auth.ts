// Authentication configuration
export const AUTH_CONFIG = {
  // Default password
  DEFAULT_PASSWORD: 'Varia4Lyfe',
  
  // Authentication duration in milliseconds (24 hours)
  AUTH_DURATION: 24 * 60 * 60 * 1000,
  
  // Local storage keys
  STORAGE_KEYS: {
    AUTHENTICATED: 'vats_authenticated',
    AUTH_TIME: 'vats_auth_time',
  }
};
